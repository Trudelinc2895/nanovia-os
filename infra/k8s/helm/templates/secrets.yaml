# TEMPLATE ONLY — Replace with ExternalSecret or SealedSecret in production
# DO NOT apply with real secret values.
{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nanovia.labels" . | nindent 4 }}
    app.kubernetes.io/component: secrets
  annotations:
    nanovia/note: "PLACEHOLDER VALUES ONLY — replace before deploying to production"
type: Opaque
data:
  JWT_SECRET_KEY: Q0hBTkdFX01F
  DATABASE_URL: Q0hBTkdFX01F
  REDIS_PASSWORD: Q0hBTkdFX01F
  STRIPE_SECRET_KEY: Q0hBTkdFX01F
  STRIPE_WEBHOOK_SECRET: Q0hBTkdFX01F
{{- end }}
