# TEMPLATE ONLY — Replace with SealedSecret in production
# DO NOT apply this file directly to a cluster with real secret values.
# Generate a SealedSecret using: kubeseal --format yaml < secrets-template.yaml > sealed-secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: nanovia-secrets
  namespace: nanovia
  labels:
    app.kubernetes.io/name: nanovia
    app.kubernetes.io/component: secrets
    app.kubernetes.io/managed-by: kustomize
    nanovia/secret-type: template
  annotations:
    # Replace with SealedSecret in production
    nanovia/note: "PLACEHOLDER VALUES ONLY — rotate all secrets before deploying"
type: Opaque
data:
  # All values are base64-encoded "CHANGE_ME" — replace before use
  JWT_SECRET_KEY: Q0hBTkdFX01F
  DATABASE_URL: Q0hBTkdFX01F
  REDIS_PASSWORD: Q0hBTkdFX01F
  STRIPE_SECRET_KEY: Q0hBTkdFX01F
  STRIPE_WEBHOOK_SECRET: Q0hBTkdFX01F
