apiVersion: v1
kind: Service
metadata:
  name: nanovia-api-blue
  namespace: nanovia
  labels:
    app.kubernetes.io/name: nanovia-api-blue
    app.kubernetes.io/component: api
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  selector:
    app: nanovia-api
    version: blue
  ports:
    - name: http
      port: 80
      targetPort: 8010
