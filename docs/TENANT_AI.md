# Tenant AI

Tenant AI profiles are stored separately from master memory and inherit plan policy, module access and credit limits from the sandbox monetization layer.

Private tenant memory is persisted under `data/memory/tenants/{tenant_id}`.
